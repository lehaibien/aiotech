import OrderHistory from '@/features/orders/OrderHistory';

/**
 * Renders the order history page by displaying the {@link OrderHistory} component.
 */
export default function Page() {
  return <OrderHistory />;
}
