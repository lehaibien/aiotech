import { ProfileForm } from '@/features/profile/ProfileForm';

/**
 * Renders the profile form page.
 *
 * Displays the {@link ProfileForm} component for editing or viewing user profile information.
 */
export default function Page() {
  return <ProfileForm />;
}
