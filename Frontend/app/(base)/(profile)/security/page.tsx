import { SecurityForm } from '@/features/security/SecurityForm';

/**
 * Renders the security settings page with the {@link SecurityForm} component.
 */
export default function Page() {
  return <SecurityForm />;
}
